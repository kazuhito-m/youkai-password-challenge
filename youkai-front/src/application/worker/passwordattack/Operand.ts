export default class Operand {
    constructor(
        public readonly finctionType: string,
        public readonly fromPasswordText: string,
        public readonly toPasswordText: string,
    ) { }
}
